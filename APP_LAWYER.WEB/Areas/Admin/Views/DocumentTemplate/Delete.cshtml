@model APP_LAWYER.DAL.Entities.DocumentTemplate

@{
    ViewBag.Title = "Delete Document Template";
    Layout = "_Layout";
}

<h2>Delete Document Template</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>DocumentTemplate</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>

    <form asp-action="DeleteConfirmed" method="post">
        <input type="hidden" asp-for="@Model.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a href="@Url.Action("Index", "DocumentTemplate", new { id = Model.Id })">Back to Index</a>
    </form>
</div>