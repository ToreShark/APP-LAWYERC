@model NavbarCategoriesViewModel

@foreach (var category in Model.Categories)
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Category" asp-action="Details" asp-route-slug="@category.Slug">@category.Name</a>
    </li>
}